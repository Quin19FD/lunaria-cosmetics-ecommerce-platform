# >>> 8sync (managed) >>>
# Derived / machine-local — rebuilt by `8sync harness init` + codegraph. Safe to ignore:
.codegraph/
.cache/8sync/
.gs/
# Secrets — NEVER commit:
.env
.env.*
!.env.example
# KEEP COMMITTED (do NOT add here): agents/ (memory), agents/skills/, AGENTS.md, CHANGELOG.md
# <<< 8sync <<<

# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma — migration history IS tracked (migrations adopted, replacing db push)

# docker
postgres-data/

# playwright e2e
/test-results/
/playwright-report/
/playwright/.cache/
